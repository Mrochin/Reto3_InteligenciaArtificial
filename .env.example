APP_PORT=8000

# Ruta local donde está el DataSync Mock
HOST_DATASYNC_PATH=./datasync-mock

# Clave secreta para los tokens JWT
JWT_SECRET=cambia_este_secreto_largo_seguro

# Límite de peticiones por minuto (rate limiting)
RATE_LIMIT_SYNC=3/minute

# Orígenes permitidos para CORS
CORS_ORIGINS=*

# Tablas permitidas (lista blanca)
ALLOWED_TABLES=kpi_jornadas,kpi_actividades,kpi_eventos

